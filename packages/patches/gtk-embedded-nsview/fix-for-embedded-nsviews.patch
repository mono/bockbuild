--- a/gdk/quartz/gdkevents-quartz.c	2012-10-26 22:35:51.000000000 +0200
+++ b/gdk/quartz/gdkevents-quartz.c	2012-10-26 22:36:28.000000000 +0200
@@ -913,6 +913,9 @@ find_window_for_ns_event (NSEvent *nseve
                     NSView* sv = [[toplevel_impl->view subviews] objectAtIndex:i];
                     NSRect r = [sv frame];
 
+                    if (sv == toplevel_impl->layer_view)
+                      continue;
+
                     if (r.origin.x <= *x && r.origin.x + r.size.width >= *x &&
                         r.origin.y <= *y && r.origin.y + r.size.height >= *y)
                       {
